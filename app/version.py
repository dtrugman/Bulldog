"""
Defines the class Version
"""

class Version(object):
    """
    A class that is filled with version info during packaging
    """

    # These values will be replaced by the installer packer script
    VERSION = "0.0.0"
    BUILD = "BUILD"
